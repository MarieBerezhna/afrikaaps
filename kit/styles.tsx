import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    container: {
        width: '100%',
        textAlign: 'center',
    },
    level: {
        margin: 25,
        padding: 100,
        borderColor: '#ccc',
        borderWidth: 5,
        borderRadius: 25,
        alignItems: 'center',  
    },
    label: {
        fontSize: 50,
    }
});